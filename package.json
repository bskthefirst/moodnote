{
  "name": "app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'No build step'",
    "sync": "npx cap sync",
    "ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}